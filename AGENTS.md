## Agent skills

### Issue tracker

Issues live in GitHub Issues for iledesma08/v720-ask (uses `gh` CLI). See `docs/agents/issue-tracker.md`.

### Triage labels

Using the default five canonical triage roles. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout (`CONTEXT.md` + `docs/adr/` at root). See `docs/agents/domain.md`.
