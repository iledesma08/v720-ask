# Full stack (legacy viewer needs cv2/numpy/Pillow; gateway + check don't).
# Installs clean on Python 3.13. netifaces/xmltodict dropped (stdlib instead).
numpy>=1.26
opencv-python-headless>=4.8.0
Pillow>=10.0.0
tqdm>=4.64.0
